# {{project_title}}

Cohort **{{cohort_name}}**, week **{{week_padded}}**.

## Quick start

```sh
docker compose watch
```

This builds the Kiln proxy from `.kiln/proxy/`, starts your app service, and
hot-reloads both on source changes.

## Next steps

- Read `spec.md` for the week's deliverables.
- Fill out `.kiln/rubric.yml` with evidence as you go.
- Run `kiln doctor` if anything looks wrong.
