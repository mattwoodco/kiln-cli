# Rubric for {{project_title}} — week {{week_padded}}.
# This file is a placeholder. The real per-cohort rubric is fetched from the
# Kiln API during `kiln scaffold` and overwrites this file on each run.
{{rubric_yaml}}
